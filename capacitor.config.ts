import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zad.almuslim',
  appName: 'زاد المسلم',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
